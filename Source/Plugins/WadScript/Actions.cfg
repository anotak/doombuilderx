
//
// This file defines which actions there are, what description they have and
// some behaviour options. The Doom Builder core will bind to these actions
// with delegates (function pointers) where you use the BeginAction and
// EndAction attributes. This file must be named Actions.cfg and must be
// included in the plugin project as "Embedded Resource".
//

//
// Options:
//
// allowkeys:       Allows the user to bind standard keys to this action.
// allowmouse:      Allows the user to bind mouse buttons to this action.
// allowscroll:     Allows the user to bind the scrollwheel to this action.
// disregardshift:  This action will trigger regardless if Shift or Control is used.
// repeat:          BeginAction will be called for automatic key repetition.
// default:         Default key is only used when the action is loaded for the first
//                  time and the default key is not used by any other action.
//
// allowkeys and allowmouse are true by default, the others are false by default.
//

categories
{
	lua = "Lua";
}

dbxluamode
{
	title = "Lua Mode";
	category = "lua";
	description = "Switches to lua mode.";
	allowkeys = true;
	allowmouse = true;
	allowscroll = true;
}

runluascript
{
	title = "Run Lua Script";
	category = "lua";
	description = "Runs the script.";
	allowkeys = true;
	allowmouse = true;
	allowscroll = true;
}

openluascript
{
	title = "Open Lua Script";
	category = "lua";
	description = "Opens a script.";
	allowkeys = true;
	allowmouse = true;
	allowscroll = true;
}

recentlua1
{
	title = "Open Recent Script 1";
	category = "lua";
	description = "Opens a script from the recently loaded list.";
	allowkeys = true;
	allowmouse = true;
	allowscroll = true;
}
recentlua2
{
	title = "Open Recent Script 2";
	category = "lua";
	description = "Opens a script from the recently loaded list.";
	allowkeys = true;
	allowmouse = true;
	allowscroll = true;
}
recentlua3
{
	title = "Open Recent Script 3";
	category = "lua";
	description = "Opens a script from the recently loaded list.";
	allowkeys = true;
	allowmouse = true;
	allowscroll = true;
}
recentlua4
{
	title = "Open Recent Script 4";
	category = "lua";
	description = "Opens a script from the recently loaded list.";
	allowkeys = true;
	allowmouse = true;
	allowscroll = true;
}
recentlua5
{
	title = "Open Recent Script 5";
	category = "lua";
	description = "Opens a script from the recently loaded list.";
	allowkeys = true;
	allowmouse = true;
	allowscroll = true;
}
recentlua6
{
	title = "Open Recent Script 6";
	category = "lua";
	description = "Opens a script from the recently loaded list.";
	allowkeys = true;
	allowmouse = true;
	allowscroll = true;
}
recentlua7
{
	title = "Open Recent Script 7";
	category = "lua";
	description = "Opens a script from the recently loaded list.";
	allowkeys = true;
	allowmouse = true;
	allowscroll = true;
}
recentlua8
{
	title = "Open Recent Script 8";
	category = "lua";
	description = "Opens a script from the recently loaded list.";
	allowkeys = true;
	allowmouse = true;
	allowscroll = true;
}
recentlua9
{
	title = "Open Recent Script 9";
	category = "lua";
	description = "Opens a script from the recently loaded list.";
	allowkeys = true;
	allowmouse = true;
	allowscroll = true;
}